version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: vitrine-postgres
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: vitrine
      POSTGRES_PASSWORD: vitrine_password
      POSTGRES_DB: vitrine_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U vitrine']
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
